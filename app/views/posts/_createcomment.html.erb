<%= form_with url: "/posts/#{@post.id}/comments", scope: :comment do |form| %>
    <div >
        <%= form.text_area :text, placeholder: "Write your comment", rows: 14, required: true %>
        <%= form.submit "Comment" %>
    </div>
<% end %>